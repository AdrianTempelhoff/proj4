<%= form_for @basket, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :temp_basket_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :temp_basket_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :purchase_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :purchase_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                baskets_path, :class => 'btn' %>
  </div>
<% end %>
