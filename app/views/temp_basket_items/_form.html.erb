<%= form_for(@temp_basket_item) do |f| %>
  <% if @temp_basket_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_basket_item.errors.count, "error") %> prohibited this temp_basket_item from being saved:</h2>

      <ul>
      <% @temp_basket_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :temp_basket_id %><br />
    <%= f.number_field :temp_basket_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br />
    <%= f.number_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
