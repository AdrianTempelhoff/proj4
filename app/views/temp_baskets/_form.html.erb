<%= form_for(@temp_basket) do |f| %>
  <% if @temp_basket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_basket.errors.count, "error") %> prohibited this temp_basket from being saved:</h2>

      <ul>
      <% @temp_basket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :basket_id %><br />
    <%= f.number_field :basket_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
